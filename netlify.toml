[build]
  command = "npm run build"
  publish = "dist/urbancompany/browser"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (also covered by src/_redirects, kept here as a backup)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
